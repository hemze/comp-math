<TeXmacs|1.0.7.16>

<style|generic>

<\body>
  <strong|#1<em|>>

  <\math>
    f<around*|(|a,b,c|)> = <frac|a*b+b*log<around*|(|<frac|c+2*a|a<rsup|>>|)>|ac+b<rsup|2>>

    a = 5.78\<nocomma\> <around*|(|\<Delta\>a=0.01|)>; b=4,38
    <around*|(|\<Delta\>b=0.01|)>; c=-5,9 <around*|(|\<Delta\>c=0.1|)>

    \;
  </math>

  <\enumerate>
    <em|><item><math|a*c=>-34.102

    <math|\<Delta\>a*c=<around*|\||a|\|>*\<Delta\>c+<around*|\||c|\|>*\<Delta\>a=0.637>

    <item><math|a*b=25.3164>

    <math|\<Delta\>a*b=<around*|\||a|\|>*\<Delta\>b+<around*|\||b|\|>*\<Delta\>a=0.1016>

    <item><math|b<rsup|2>=19.1844>

    <math|\<Delta\>b<rsup|2>=2*<around*|\||b|\|>*+\<Delta\>b=0.0876>

    <item><math|<around*|(|1|)>+<around*|(|3|)>=-14.9176>

    <math|\<Delta\><around*|(|<around*|(|1|)>+<around*|(|3|)>|)>=\<Delta\><around*|(|1|)>+\<Delta\><around*|(|3|)>=0.0001>

    <item><math|*g<around*|(|a,c|)>=log<around*|(|<frac|c+2*a|a*>|)>>

    <math|g<around*|(|5.78,-5.9|)>=-0.02097979046919452>

    <math|\<Delta\>g=max<around*|\||g<rprime|'><rsub|a>*|\|>\<Delta\>a+max<around*|\||g<rprime|'><rsub|c>|\|>*\<Delta\>c=-<frac|c-a|a*c+3*a<rsup|2>>=0.176639969>

    <item><math|\<Delta\><around*|(|b*log<around*|(|<frac|c+2*a|a*>|)>|)>=<around*|\||log<around*|(|<frac|c+2*a|a*>|)>|\|>*\<Delta\>b+<around*|\||b|\|>*\<Delta\><around*|(|log<around*|(|<frac|c+2*a|a*>|)>|)>=0.397643>

    <item><math|\<Delta\><around*|(|<around*|(|2|)>+<around*|(|6|)>|)>=\<Delta\><around*|(|2|)>+\<Delta\><around*|(|6|)>=0.099227>

    <item><math|f<around*|(|5.78,4.38,-5.9|)>=-1.690922703232753>

    <math|\<Delta\><around*|(|<frac|<around*|(|7|)>|<around*|(|4|)>>|)>=<frac|<around*|\||<around*|(|7|)>|\|>*\<Delta\><around*|(|4|)>+<around*|\||<around*|(|4|)>|\|>*\<Delta\><around*|(|7|)>|<around*|(|4|)><rsup|2>>=>

    <math|\<delta\>f=<frac|\<Delta\>f|<around*|\||f|\|>>=>
  </enumerate>
</body>

<\initial>
  <\collection>
    <associate|font|cyrillic>
    <associate|language|russian>
    <associate|page-type|letter>
  </collection>
</initial>